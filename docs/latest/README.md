This directory is intentionally shipped without synthetic demo rasters.

Run one of the real commands below to populate it with actual Copernicus-based outputs:

- `PYTHONPATH=backend python -m seydyaar run-daily ...`
- `PYTHONPATH=backend python -m seydyaar run-seasonal ...`
